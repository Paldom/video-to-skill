# CI/dev tooling only — everything this repo *ships* stays stdlib-only, because
# skills run on a user's machine after `npx skills add` and must not drag in deps.
# Pins must match .pre-commit-config.yaml so local and CI never disagree.
# Dependabot (pip ecosystem) keeps these fresh.
check-jsonschema==0.37.4
ruff==0.15.10
pre-commit==4.6.2
